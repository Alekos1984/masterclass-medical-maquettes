{
  "name": "masterclass-medical",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "24.x",
    "npm": "10.x"
  },
  "scripts": {
    "install": "cd web && npm install --include=dev",
    "build": "cd web && npm run build && npm prune --production",
    "start": "cd web && npm start"
  }
}
